import random\n\n# Function to generate the trading robot configuration\ndef generate_robot(id):\n    mass = random.uniform(0.1, 10.0)  # Mass between 0.1 and 10.0\n    sensitivity = random.uniform(1.0, 5.0)  # Sensitivity between 1.0 and 5.0\n    volume = random.uniform(0.01, 1.0)  # Volume between 0.01 and 1.0\n    return f"Robot ID: {id}\nMass: {{mass}} kg\nSensitivity: {{sensitivity}}\nVolume: {{volume}} m³\n"\n\n# Generate 50 trading robots\nfor i in range(1, 51):\n    robot_config = generate_robot(i)\n    with open(f'trading_robot_{i}.txt', 'w') as f:\n        f.write(robot_config)\n